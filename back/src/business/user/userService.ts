/*import { Context } from 'node:vm';

class UserService {
  async loginUser(context: Context, userLoginData: UserLoginData): LoginData {

  }

  async createUser(context: Context): CreateUserData {}
}

export default new UserService();
*/
